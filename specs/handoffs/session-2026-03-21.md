# Temporal Session Handoff (2026-03-21)

## Scope Completed

- Closed the Phase H preview workflow issues around timer startup,
  listener gating, main-button lifecycle ordering, and chart/3D drift.
- Reworked preview data flow so charts and 3D consume one shared
  `trackingFrames` timeline while the right sidebar keeps stable source rows.
- Updated Phase H and UI specs so the preview contract now documents:
  shared tracking time-series, unchecked-row semantics, listener gating,
  and the current direction toward runtime-aligned normalized models.
- Confirmed with the user that preview should remain stable demo data,
  should converge toward the same normalized runtime schema,
  should not yet simulate abnormal scenarios,
  and should eventually carry per-frame dynamic `energy`
  for UI and state-machine verification.

## Key Commits

- `4873dcd` `fix(preview): align bridge workflow and docs`

## Validation Performed

- `uv run pyright`
  - Passed with `0 errors` and `1 warning`
  - Remaining warning: unresolved source import for `paramiko`
- `uv run ruff check src tests`
  - Passed
- `uv run pyside6-qmllint src/temporal/qml/Main.qml`
  - Passed
- `uv run python -m unittest tests.test_preview_bridge`
  `tests.test_app_bridge_recording -v`
  - Passed

## Changed Files

- `specs/phase-progress.md`
- `specs/handoffs/session-2026-03-21.md`
- `specs/plans/phase-h-preview-data-linkage.md`
- `specs/plans/phase-h-preview-filtering-and-validation.md`
- `specs/ui/left-sidebar.md`
- `specs/ui/right-sidebar.md`
- `specs/ui/center-pane.md`
- `specs/ui/source-sphere-view.md`
- `docs/runbook.md`
- `docs/risks.md`
- `src/temporal/app.py`
- `src/temporal/core/config_loader.py`
- `src/temporal/preview_bridge.py`
- `src/temporal/preview_data.py`
- `src/temporal/preview_main.py`
- `src/temporal/qml/LeftSidebar.qml`
- `tests/test_app_bridge_recording.py`
- `tests/test_preview_bridge.py`

## Assumptions

- Preview remains a stable demo-data path rather than a real capture
  replay path.
- Preview and runtime should ultimately share one normalized tracking schema,
  even if preview data is still generated deterministically in Python fixtures.
- This phase continues to prioritize UI consistency and state-machine
  consistency over abnormal-scene fidelity.
- Dynamic per-frame `energy` is the next required model step for exercising
  right-sidebar refresh logic, but it is not yet implemented in code here.

## Unresolved Risks

- Preview documentation now records per-frame dynamic `energy`
  as the next model step, so the current implementation still has a gap
  between documented direction and shipped bridge behavior.
- Manual end-to-end validation in the real preview window still needs another
  pass after the next model change, especially around right-sidebar number
  updates and filter coupling.
- Remote odaslive end-to-end validation has not yet been rerun after the
  latest preview follow-up decisions.

## Next Session First Commands

1. `git status --short --untracked-files=all`
2. `uv run pyright`
3. `uv run python -m unittest tests.test_preview_bridge -v`
4. `uv run temporal-preview`

## Suggested First Tasks

1. Add per-frame `energy` into preview `trackingFrames`
   and wire the right-sidebar numeric display to the current frame.
2. Add regression tests that prove right-sidebar dynamic numbers, charts,
   x-axis ticks, and 3D positions advance on the same preview clock.
3. Re-run manual preview validation for `referenceSingle`,
   `hemisphereSpread`, `equatorBoundary`, and `emptyState`
   after the energy-model follow-up lands.
