# Temporal Session Handoff (2026-03-20)

## Scope Completed

- 统一了治理文档的语言归属和规则去重方向，明确 `specs/` 与
  `.github/copilot-instructions.md` 的职责边界。
- 补充了 root-cause-first 治理约束，要求先澄清、再回到第一性原理修设计，
  最后用闭环测试收口。
- 调研了 `D:\Workspace\odas_web` 的默认监听行为，确认它未显式传 host，
  等价于 bind 所有本地接口，而不是只监听 `127.0.0.1`。
- 基于该结论，修正了 Temporal 里的 `listen_host` 默认值、wildcard 语义、
  远端 preflight 规则，以及对应测试。
- 完成了 Phase H preview entrypoint 和 shared bridge baseline，
  使 `uv run temporal-preview` 可通过 `PreviewBridge` 启动同一主窗口。
- 去掉了 header、center pane、right sidebar 和 source sphere
  里的 QML 本地 preview 分支，让 preview/runtime 差异统一由 bridge 输出。
- 将 preview 场景联动提升为 bridge 驱动模型，使 header 切换器、
  右栏、图表和 3D 共用同一场景数据。
- 识别并修正了 preview linkage 根因：
  sidebar rows 被错误收缩成了图表/3D 的可见 source 集。

## Key Commits

- `8799e77` `governance: align language rule ownership`
- `0483682` `governance: add root-cause fix prompt`
- `4fdabbb` `fix(remote): listen for odas streams locally`
- `4f86c29` `fix(remote): default stream listener to wildcard bind`
- `993e619` `phase-h: add preview entrypoint and bridge`
- `4c99a5d` `phase-h: link preview scenarios through explicit bridge injection`
- `a0f27e3` `phase-h: align preview sidebar semantics with runtime`

## Validation Performed

- `npx markdownlint specs/handoffs/session-2026-03-18.md`
- `npx markdownlint specs/handoffs/session-2026-03-19.md`
- `npx markdownlint specs/handoffs/session-2026-03-20.md`
- `npx markdownlint specs/in-progress.md`
- `npx markdownlint specs/plans/phase-e-agent-governance.md`
- `npx markdownlint-cli specs/plans/phase-h-preview-entry-and-bridge.md`
- `npx markdownlint-cli specs/plans/phase-h-preview-data-linkage.md`
- `npx markdownlint-cli specs/ui/app-header.md`
- `npx markdownlint-cli specs/ui/center-pane.md`
- `npx markdownlint-cli specs/ui/right-sidebar.md`
- `npx markdownlint-cli specs/ui/main-window.md`
- `npx markdownlint-cli specs/ui/source-sphere-view.md`
- `uv run ruff check src tests`
- `uv run ruff format src tests`
- `uv run pyright src tests`
  - 结果：仅剩 `paramiko` 的 `reportMissingModuleSource` warning。
- `uv run python -m unittest tests.test_config_loader tests.test_odas_stream_client -v`
- `uv run python -m unittest discover -s tests -p "test_*.py" -v`
- `uv run python -m unittest tests.test_preview_bridge -v`
- Elevated startup smoke check:
  - `uv run temporal-preview`
  - `uv run temporal`

## Changed Files

- specs/plans/phase-e-agent-governance.md
- docs/runbook.md
- specs/plans/phase-b-remote-control-and-streams.md
- specs/in-progress.md
- specs/phase-progress.md
- specs/handoffs/session-2026-03-20.md
- specs/plans/phase-h-preview-data-linkage.md
- specs/ui/center-pane.md
- specs/ui/right-sidebar.md
- src/temporal/core/config_loader.py
- src/temporal/core/ssh/remote_odas.py
- src/temporal/preview_bridge.py
- tests/test_config_loader.py
- tests/test_odas_stream_client.py
- tests/test_remote_odas.py
- tests/test_preview_bridge.py

## Assumptions

- 历史 handoff 文档允许按批次逐步归一，不要求一次性清理全部旧文件。
- 当前治理修正仍然是仓库规则的有效基线。
- Remote ODAS 继续采用 `ODAS -> Temporal` 的主动推流方向。
- `0.0.0.0` 和 `::` 只表示本地 bind wildcard，
  远端 sink 配置仍应填写真实可达的 Temporal IP。
- Preview sidebar rows 应表示完整场景 source catalog。
- Charts 和 3D 应只消费当前可见/已勾选的 preview source 集合。
- `暂无活动声源` 只应表示场景本身为空，
  不应表示用户在非空场景里取消了全部勾选。

## Unresolved Risks

- 更早的 handoff 文档可能仍有语言和格式漂移，后续还要继续做批量归一。
- `_common_shell_prelude` 在多个 SSH 脚本里重复拼接，仍有性能损失。
- 远端 cfg sink 校验仍是文本启发式，不是结构化 ODAS cfg 解析。
- Manual screenshot-based acceptance of the current preview UI
  had not yet been completed for all baseline scenarios at this stage.
- Preview filtering semantics for `sourcesEnabled` / `potentialsEnabled`
  were still deferred to the remaining Phase H filtering work at this stage.

## Next Session First Commands

1. `git status --short --untracked-files=all`
2. `uv run ruff check src tests`
3. `uv run pyright src tests`
4. `uv run python -m unittest tests.test_preview_bridge -v`
5. `uv run temporal-preview`

## Suggested First Tasks

1. 拆分或缓存 `RemoteOdasController` 的 `_common_shell_prelude`，
   保持行为不变并补测试。
2. 手工验证 `referenceSingle`、`hemisphereSpread`、
   `equatorBoundary` 和 `emptyState`，
   记录 preview 视觉与联动剩余偏差。
3. 实现剩余的 Phase H filtering semantics，
   使 sidebar、charts 和 3D 对 energy-range controls 保持一致响应。
